<?xml version="1.0" encoding="utf-8" ?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!--
          Sprint 87 Phase 0 — FCM display defaults. Used by
          @capacitor/push-notifications' bundled MessagingService (merged
          into this manifest from the plugin AAR) when a push arrives:
          the OS tray uses these to render the notification icon + tint
          and to route it into the correct NotificationChannel.

          `default_notification_channel_id` MUST match the channel id
          created in MainActivity.onCreate() below. `default_notification_icon`
          must be a monochrome white-on-transparent drawable (Android
          renders every non-white pixel as a solid white square on the
          status bar from API 21+).

          These entries are inert until google-services.json + Firebase
          project are provisioned in Phase 1; assembleDebug is
          unaffected by their presence.
        -->
        <meta-data android:name="com.google.firebase.messaging.default_notification_channel_id" android:value="kamizo_default" />
        <meta-data android:name="com.google.firebase.messaging.default_notification_icon"       android:resource="@drawable/ic_stat_kamizo" />
        <meta-data android:name="com.google.firebase.messaging.default_notification_color"      android:resource="@color/brand_orange" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Photo attachment on the resident "new request" / chat / marketplace /
      announcements / meeting flows uses <input type="file" accept="image/*"
      capture="environment"> in the WebView. On modern Android:
       - Camera path: still needs CAMERA (below) — for capturing a new photo.
       - Gallery path: automatically routes to the SYSTEM PHOTO PICKER
         on Android 13+ (backported to 4.1+ via WebView) WHEN we DO NOT
         declare READ_MEDIA_IMAGES / READ_EXTERNAL_STORAGE. Declaring them
         forces the legacy permission-gated chooser AND is now rejected
         by Google Play policy (photo-and-video permissions). Photo Picker
         is scoped to only the images the user picks — no runtime dialog,
         no full-gallery access, Play-compliant.
    -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Explicit removal directives — if any bundled library adds these
         back through manifest merge, strip them so Play doesn't reject
         the release. READ_MEDIA_VIDEO is defensive; no current library
         adds it, but keeping the rule prevents future regressions. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"      tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"       tools:node="remove" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"  tools:node="remove" />

    <!-- Camera preferred but not required (some emulators / cheap
         tablets ship without one). `required=false` lets the APK install
         everywhere; runtime falls back to gallery picker. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- v118.35 — Android 13 (API 33+) requires this runtime permission
         for any app that posts notifications. Kamizo ships @capacitor/
         push-notifications and the registration call silently fails
         on Android 13+ without it. Without this declaration Google
         Play also flags the listing in the Data Safety form review. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
