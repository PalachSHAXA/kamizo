.DS_Store
node_modules/
dist/
.env
.env.local
.dev.vars
.wrangler/
*.timestamp-*

# Build artifacts (deployed via wrangler, not needed in git)
cloudflare/public/assets/
cloudflare/public/index.html

# Audit/doc files (generated, not source)
ARCHITECTURE_DIAGRAM.txt
AUDIT_FILES_CHECKLIST.txt
AUDIT_SUMMARY.txt
CHANGELOG_AUDIT_FIX.html
PROJECT_INDEX.md
README_AUDIT.md
ROUTE_AUDIT.md
ROUTE_QUICK_REFERENCE.txt

# Mobile build artifacts
*.apk

# Capacitor native projects (android/ and ios/) ship their own
# Capacitor-managed .gitignore files; we commit the project skeleton
# and let those exclude build/, Pods/, .gradle/, DerivedData, etc.

# IDE / local editor state
.vscode/
.idea/

# Claude Code local backup/state files
.claude/settings.json.bak
.claude/settings.local.json