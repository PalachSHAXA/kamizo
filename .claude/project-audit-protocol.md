# 🔍 UK CRM - Автоматический Протокол Аудита

## 📋 ОБЯЗАТЕЛЬНЫЙ ЧЕКЛИСТ ДЛЯ КАЖДОГО ПРОМПТА

Перед выполнением любой задачи необходимо провести полный аудит проекта по следующим критериям:

---

## 1️⃣ ПРОИЗВОДИТЕЛЬНОСТЬ И СКОРОСТЬ

### Frontend Performance
- [ ] Проверить размер бандла (должен быть < 500KB gzipped)
- [ ] Проверить code splitting и lazy loading
- [ ] Найти неоптимальные re-renders в React компонентах
- [ ] Проверить использование useMemo, useCallback
- [ ] Проверить размер изображений и assets
- [ ] Проверить наличие виртуализации для длинных списков

### Backend Performance
- [ ] Проверить N+1 запросы в SQL
- [ ] Проверить индексы в базе данных D1
- [ ] Проверить кэширование (cache TTL, invalidation)
- [ ] Проверить время выполнения Workers (должно быть < 50ms)
- [ ] Проверить использование Durable Objects (оптимальность)

### Network Performance
- [ ] Проверить количество HTTP запросов
- [ ] Проверить размер API ответов
- [ ] Проверить использование WebSocket vs polling
- [ ] Проверить compression (gzip/brotli)

---

## 2️⃣ МЕРТВЫЙ КОД (Dead Code)

### Найти и удалить:
- [ ] Неиспользуемые импорты
- [ ] Неиспользуемые функции и компоненты
- [ ] Неиспользуемые переменные
- [ ] Закомментированный код
- [ ] Дублирующийся код (DRY principle)
- [ ] Неиспользуемые зависимости в package.json
- [ ] Неиспользуемые CSS классы
- [ ] Неиспользуемые API endpoints

### Инструменты для проверки:
```bash
# Frontend
npx depcheck  # Неиспользуемые зависимости
npx ts-prune  # Неиспользуемые exports

# Bundle analysis
npm run build -- --stats
npx vite-bundle-visualizer
```

---

## 3️⃣ КОНФЛИКТНЫЕ ЗАВИСИМОСТИ

### Проверить:
- [ ] Конфликты версий в package.json
- [ ] Дублирующиеся зависимости разных версий
- [ ] Устаревшие зависимости (security vulnerabilities)
- [ ] Peer dependency warnings
- [ ] Совместимость TypeScript версий

### Команды:
```bash
npm ls  # Показать дерево зависимостей
npm outdated  # Показать устаревшие пакеты
npm audit  # Проверить уязвимости
npm dedupe  # Удалить дубликаты
```

---

## 4️⃣ АСИНХРОННОСТЬ И RACE CONDITIONS

### Проверить:
- [ ] Правильное использование async/await
- [ ] Обработка ошибок в промисах (try/catch)
- [ ] Race conditions в WebSocket/SSE
- [ ] Race conditions в Zustand store
- [ ] Правильная последовательность запросов (зависимости)
- [ ] Отмена запросов при unmount (AbortController)
- [ ] Debouncing/throttling для частых событий
- [ ] Optimistic updates (оптимистичные обновления UI)

### Паттерны для проверки:
```typescript
// ❌ BAD: Race condition
const [data, setData] = useState();
useEffect(() => {
  fetchData().then(setData);  // Может вернуться после unmount
}, []);

// ✅ GOOD: С отменой
useEffect(() => {
  const controller = new AbortController();
  fetchData({ signal: controller.signal }).then(setData);
  return () => controller.abort();
}, []);
```

---

## 5️⃣ МОДУЛЬНОСТЬ И АРХИТЕКТУРА

### Проверить структуру:
- [ ] Соблюдение принципа единственной ответственности (SRP)
- [ ] Правильное разделение на слои (presentation, business logic, data)
- [ ] Размер файлов (не более 300-500 строк)
- [ ] Цикличные зависимости между модулями
- [ ] Правильное использование barrel exports (index.ts)
- [ ] Соблюдение принципа инверсии зависимостей

### Инструменты:
```bash
npx madge --circular src/  # Найти циклические зависимости
npx ts-unused-exports  # Найти неиспользуемые exports
```

---

## 6️⃣ ОБНОВЛЕНИЕ ВЕРСИЙ

### Cloudflare Wrangler
```bash
cd cloudflare
npm install -D wrangler@latest
npx wrangler --version
```

### Frontend Dependencies
```bash
cd src/frontend
npm update
npm outdated  # Проверить что можно обновить
```

### Критичные обновления:
- [ ] React (текущая: 18.x → проверить latest)
- [ ] TypeScript (текущая: 5.9.3 → проверить latest)
- [ ] Vite (текущая: 7.3.0 → проверить latest)
- [ ] Wrangler (текущая: 3.114.16 → обновить до 4.x)
- [ ] Zustand (текущая: 5.0 → проверить latest)

---

## 7️⃣ MEMORY LEAKS И УТЕЧКИ ПАМЯТИ

### Проверить:
- [ ] Event listeners без cleanup
- [ ] setInterval/setTimeout без clearInterval/clearTimeout
- [ ] WebSocket/SSE соединения без закрытия
- [ ] Subscriptions (Zustand) без unsubscribe
- [ ] Большие объекты в замыканиях
- [ ] Неочищаемые кэши

### Паттерны:
```typescript
// ❌ BAD: Memory leak
useEffect(() => {
  window.addEventListener('resize', handleResize);
}, []);

// ✅ GOOD: Cleanup
useEffect(() => {
  window.addEventListener('resize', handleResize);
  return () => window.removeEventListener('resize', handleResize);
}, []);
```

---

## 8️⃣ БЕЗОПАСНОСТЬ (Security Audit)

### Проверить:
- [ ] SQL Injection защита (параметризованные запросы)
- [ ] XSS защита (sanitization)
- [ ] CSRF токены
- [ ] Rate limiting работает корректно
- [ ] Правильная валидация на backend
- [ ] Secrets не в коде (используются env переменные)
- [ ] CORS настроен правильно
- [ ] Хеширование паролей (PBKDF2-SHA256)

### Команды:
```bash
npm audit fix  # Исправить уязвимости
npm audit  # Показать все уязвимости
```

---

## 9️⃣ ТИПИЗАЦИЯ И КАЧЕСТВО КОДА

### TypeScript
- [ ] Нет использования `any`
- [ ] Все функции имеют возвращаемый тип
- [ ] Правильные generic типы
- [ ] Нет @ts-ignore без комментариев
- [ ] Strict mode включен

### Code Quality
- [ ] ESLint warnings = 0
- [ ] Prettier форматирование
- [ ] Консистентный naming convention
- [ ] JSDoc комментарии для сложной логики

---

## 🔟 МОНИТОРИНГ И ЛОГИРОВАНИЕ

### Проверить:
- [ ] Error boundaries в React
- [ ] Centralized error handling
- [ ] Логирование критичных операций
- [ ] Performance metrics (Web Vitals)
- [ ] Cloudflare Analytics настроен

---

## 🚀 АВТОМАТИЧЕСКИЙ СКРИПТ АУДИТА

Создать файл `.claude/run-audit.sh`:

```bash
#!/bin/bash

echo "🔍 UK CRM - Автоматический Аудит"
echo "================================"

# 1. Dead Code
echo "📦 Проверка мертвого кода..."
cd src/frontend
npx depcheck
npx ts-prune

# 2. Dependencies
echo "📦 Проверка зависимостей..."
npm outdated
npm audit

# 3. Bundle Size
echo "📦 Проверка размера бандла..."
npm run build
du -sh dist/

# 4. Circular Dependencies
echo "🔄 Проверка циклических зависимостей..."
npx madge --circular src/

# 5. TypeScript
echo "📝 Проверка TypeScript..."
npx tsc --noEmit

# 6. Linting
echo "✨ Проверка ESLint..."
npx eslint src/

# 7. Backend
echo "⚙️ Проверка Backend..."
cd ../../cloudflare
npm outdated
npm audit

echo "✅ Аудит завершен!"
```

---

## 📊 BENCHMARKING

### Frontend Performance
```javascript
// Измерить время рендера
performance.mark('render-start');
// ... рендер компонента
performance.mark('render-end');
performance.measure('render', 'render-start', 'render-end');
```

### Backend Performance
```typescript
// В Workers
const start = Date.now();
// ... выполнение логики
const duration = Date.now() - start;
console.log(`Request took ${duration}ms`);
```

---

## 🎯 КРИТЕРИИ УСПЕХА

### Performance Targets
- ✅ Frontend bundle < 500KB gzipped
- ✅ First Contentful Paint < 1.5s
- ✅ Time to Interactive < 3s
- ✅ API response time < 100ms (p95)
- ✅ Worker cold start < 50ms

### Code Quality Targets
- ✅ 0 ESLint errors
- ✅ 0 TypeScript errors
- ✅ 0 high/critical npm audit vulnerabilities
- ✅ 0 циклических зависимостей
- ✅ Test coverage > 80%

---

## 📝 ОТЧЕТ ПОСЛЕ КАЖДОГО ПРОМПТА

После выполнения задачи создавать отчет:

```markdown
## Аудит выполнен: [ДАТА]

### ✅ Исправлено:
- [список исправлений]

### ⚠️ Требует внимания:
- [список проблем]

### 📊 Метрики:
- Bundle size: XXX KB
- Dependencies: XXX (XXX outdated)
- TypeScript errors: 0
- Performance: XXms

### 🔄 Следующие шаги:
- [рекомендации]
```

---

## 🔄 ПЕРИОДИЧЕСКОЕ ОБНОВЛЕНИЕ

### Еженедельно:
```bash
npm update  # Обновить minor/patch версии
npm audit fix  # Исправить уязвимости
```

### Ежемесячно:
```bash
npm outdated  # Проверить major обновления
# Обновить major версии вручную с тестированием
```

---

## 🛠️ ИНСТРУМЕНТЫ ДЛЯ АВТОМАТИЗАЦИИ

### Рекомендуемые пакеты:
```json
{
  "devDependencies": {
    "depcheck": "latest",
    "ts-prune": "latest",
    "madge": "latest",
    "vite-bundle-visualizer": "latest",
    "lighthouse": "latest",
    "eslint": "latest",
    "prettier": "latest"
  }
}
```

---

**ЭТОТ ПРОТОКОЛ ДОЛЖЕН ВЫПОЛНЯТЬСЯ ПЕРЕД КАЖДЫМ ПРОМПТОМ И ПОСЛЕ ВЫПОЛНЕНИЯ ЗАДАЧИ!**
