-- Migration: Add password_changed_at field for onboarding tracking
-- This allows tracking when user changed their password across devices

ALTER TABLE users ADD COLUMN password_changed_at TEXT;

-- Create index for faster queries
CREATE INDEX IF NOT EXISTS idx_users_password_changed ON users(password_changed_at);
